# Notebooks

Exploratory notebooks may be used for model, retrieval and evaluation experiments. Production logic should be moved into the `src/enterprise_copilot` package so the final system remains testable and reusable.
