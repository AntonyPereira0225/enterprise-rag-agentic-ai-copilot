# Configuration

Configuration files for retrieval, model, evaluation and application behaviour will live here. Environment-specific secrets belong in local environment variables and must not be committed.
