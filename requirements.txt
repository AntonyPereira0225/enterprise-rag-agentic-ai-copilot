# The complete offline application uses only the Python 3.11+ standard library.
# Install an optional requirements file only for the adapter you want:
#   requirements-api.txt   FastAPI / Uvicorn
#   requirements-dense.txt Sentence Transformers / FAISS
#   requirements-mlops.txt MLflow and delivery validation
